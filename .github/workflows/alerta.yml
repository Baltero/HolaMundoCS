name: Notificación a ntfy.sh

on:
  push:
    branches:
      - main

jobs:
  notificar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Enviar notificación a ntfy.sh
        run: |
          curl -H "Title: 🚀 Commit enviado por Baltero" \
               -H "Priority: high" \
               -d "Baltero ha subido un nuevo commit a la rama main del repositorio HolaMundoCS. ¡Todo marcha bien! 🎯" \
               https://ntfy.sh/devops-itla
