name: Notificación a ntfy.sh

on:
  push:
    branches:
      - main

jobs:
  notificar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Enviar notificación a ntfy.sh
        run: |
          curl -H "Title: Push a main por ${{ Robertson(Baltero) }}" \
               -H "Priority: high" \
               -d "El usuario ${{ Robertson(Baltero) }} ha subido código nuevo a la rama main del repositorio HolaMundoCS." \
               https://ntfy.sh/devops-itla
